# Arc Agent Money

Arc gives stablecoin builders a focused home for USDC velocity. Arcanum adds the governance envelope autonomous agents need when that velocity becomes operational.

The core idea is simple: agents should be able to act, and operators should be able to define the exact envelope where that action is safe.
