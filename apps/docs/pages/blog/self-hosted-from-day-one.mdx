# Self-hosted From Day One

Arcanum ships with an npm-first web workflow, Supabase-backed configuration, and optional Helm scaffolding because open infrastructure should be easy to review and run.

Self-hosting is also a product principle. The indexer and dashboard help operators see what is happening, but the protocol layer stays on Arc.
