# VendorRegistry

VendorRegistry stores per-wallet vendor status, category, cap, and metadata hash. It is a shared registry contract with wallet-scoped namespaces.

Categories:

- API
- COMPUTE
- DATA
- SUBCONTRACTING
- OTHER

Blocked vendors return a freeze verdict from PolicyEngine.
